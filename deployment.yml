apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: testingproject
spec:
 replicas: 3
 selector:
   matchLabels:
      app: frontend
 template:
     metadata:
       labels:
         app: frontend
     spec:
       containers:
        - name: frontendimg
          image: heyrohhh/testjenkinsv3:IMAGE_TAG
          ports:
           - containerPort: 8080
          resources:
             requests:
                  cpu: 128m
                  memory: 256Mi
             limits:
                 cpu: 256m
                 memory: 512Mi
---

apiVersion: v1
kind: Service
metadata:
  name: frontendservice
  namespace: testingproject
spec:
  type: LoadBalancer
  selector:
     app: frontend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
